[project]
name = "evaluator"
version = "0.1.0"
description = "ADA quality evaluation loop — collects monthly quality signals into a dashboard-ready data layer + human report"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]
